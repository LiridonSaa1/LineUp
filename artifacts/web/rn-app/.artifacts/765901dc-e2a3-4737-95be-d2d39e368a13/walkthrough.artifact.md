# Walkthrough - Fresh Supabase Setup

I have cleaned up the application's data management and prepared the SQL necessary to reset your Supabase instance to a clean state.

## Changes Made

### 1. Code Cleanup
- **Removed Seeding Logic**: Modified `HomeScreen.tsx` to stop attempting to `upsert` default advertisement data every time the app loads. The app now strictly reads from the `advertisements` table. This prevents potential errors if you manually change data in Supabase and makes the app start up slightly faster.

### 2. Database Reset Script (Action Required)
I have prepared a comprehensive SQL script to reset your database and set up the new `categoria` structure.

> [!CAUTION]
> Running this script will delete all users, barbershops, and appointments permanently.

**SQL Script to run in Supabase:**
```sql
-- 1. Drop tables in order of dependency
DROP TABLE IF EXISTS subscriptions CASCADE;
DROP TABLE IF EXISTS customers CASCADE;
DROP TABLE IF EXISTS appointments CASCADE;
DROP TABLE IF EXISTS user_locations CASCADE;
DROP TABLE IF EXISTS recent_searches CASCADE;
DROP TABLE IF EXISTS barbershops CASCADE;
DROP TABLE IF EXISTS users CASCADE;

-- 2. Create the 'categoria' table
CREATE TABLE categoria (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  name TEXT NOT NULL UNIQUE,
  icon TEXT,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- 3. Seed 'categoria' with homepage categories
INSERT INTO categoria (name, icon) VALUES
('Haircut & Styling', 'Scissors'),
('Hair Coloring', 'Sparkles'),
('Hair Treatment', 'Zap'),
('Beard & Grooming', 'User'),
('Nails', 'Hand'),
('Makeup', 'Smile'),
('Brows & Lashes', 'Eye'),
('Skin Care', 'Shield'),
('Body Care', 'Waves')
ON CONFLICT (name) DO NOTHING;
```

## Verification Results
- [x] `HomeScreen.tsx` logic verified: It now fetches live ads from the DB without attempting to create them.
- [x] SQL Script structure verified for Supabase compatibility.

render_diffs(file:///C:/Users/lirid/OneDrive/Desktop/LineUp/artifacts/web/rn-app/src/screens/HomeScreen.tsx)
